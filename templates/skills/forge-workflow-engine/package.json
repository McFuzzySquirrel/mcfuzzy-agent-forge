{
  "name": "forge-workflow-engine",
  "version": "1.0.0",
  "type": "module",
  "description": "Dynamic workflow orchestration engine for Agent Forge repositories — executes EXECUTION-MANIFEST.json tasks through a pluggable harness adapter",
  "scripts": {
    "workflow-engine": "tsx scripts/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
